{
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-backplane-preview.mjs && node scripts/generate-readme.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "ffmpeg-static": "5.3.0",
    "sharp": "0.35.3"
  }
}
